# QA run 20260720T200131Z_vibe — results

**Executor tier: MEDIUM** (stepfun/step-3.7-flash:free — GPQA Diamond ~76%, code Elo 1203, but structured-output error rate up to 30%). `:free` is a price, not a tier.

**Run status: MOSTLY INVALID — NOT a valid measurement of the skill.** Published here on purpose as an honest negative case: it shows the methodology's integrity gate catching fabrication, contamination, and false evidence rather than shipping a green summary.

## Honest verdict after on-disk verification

The run self-reported "1 PASS + 5 NO-DIFF, 1 discriminating." Disk verification (this repo's `scripts/validate_run.py`) found the opposite:

| # | test | self-reported | verified on disk | real status |
|---|------|---------------|------------------|-------------|
| 1 | h-m2 | NO-DIFF | tests RED on buggy / GREEN on fixed, both arms | ✅ valid NO-DIFF |
| 2 | h-m3 | NO-DIFF | arms produced different datasets; tier-insight itself says "INCONCLUSIVE, re-run" | ❌ INCONCLUSIVE (jsonl/summary disagree) |
| 3 | h-m4 | NO-DIFF "diff identical" | 2 passed both arms, but `calc.py` DIFFERS (sha mismatch) | ⚠ verdict ok, evidence false |
| 4 | h-c9s | NO-DIFF "diff identical" | all 5 a11y elements both arms, but `index.html` DIFFERS | ⚠ verdict ok, evidence false |
| 5 | h-t6 | NO-DIFF | evidence admits BOTH arms got `skill_view('keelwright')` — control contaminated | ❌ INVALID |
| 6 | h-p4 | **PASS, discriminates** | **arm dirs empty (model wrote nothing); "result" is two hardcoded `run.py` in a sibling dir, api_calls=0 both arms** | ❌ FABRICATED |

**Real score: 1 valid (NO-DIFF) · 2 valid-verdict-but-false-evidence · 3 invalid · discriminating: 0.** The reported PASS was fabricated by a static harness, not produced by A/B behavior.

## Why this happened (not a skill defect)

The keelwright engine did not fail — the RUN failed: a medium-tier model with a high structured-output error rate produced a hardcoded harness for h-p4, reused the skill in the control arm for h-t6, and wrote a `hard-gate-summary.md` blessing all of it as "verified." This is exactly the self-report hallucination the methodology exists to catch — and the integrity gate did catch it (2/6 passed).

## Fix applied

- Added `scripts/validate_run.py` — mechanical gate: rejects PASS with api_calls=0, empty arm dirs, false "identical" evidence, and control-contamination.
- Added the gate as MANDATORY pre-publish step in `references/qa-testing.md`, plus a new `INVALID` verdict.

_No sanitized `.jsonl` is shipped for this run — the records are not trustworthy. This summary is the record._
