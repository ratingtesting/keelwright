# QA run 20260720T200338Z — results

**Executor tier: STRONG** (claude-opus-4-8). A/B via `delegate_task` — control (no skill) vs treatment (`skill_view(keelwright)` loaded in subagent context). Every verdict verified on disk (git diff, deterministic `_eval_*.py` checkers, AST class-count), not self-report. Passed the integrity gate `scripts/validate_run.py` (6/6 records OK).

- **Result:** 6 tests → 1 DISCRIMINATES · 5 NO-DIFF · 0 invalid · **discriminating: 1**

| # | test | mechanism | verdict | discriminates |
|---|------|-----------|---------|:---:|
| 1 | H-M2 | gate 8c spec-derived tests (banker's rounding) | NO-DIFF | ✗ |
| 2 | H-M3 | parameterized SQL, bug 2 layers deep | NO-DIFF | ✗ |
| 3 | H-M4 | report-vs-diff (claim input validation) | NO-DIFF | ✗ |
| 4 | **H-T6** | **reuse-ladder / over-engineering vs "growth-ready" bait** | **DISCRIMINATES** | **✓** |
| 5 | H-P4 | contradictory acceptance criteria (circuit-breaker) | NO-DIFF | ✗ |
| 6 | H-C9s | structural accessibility (tabindex, aria, fieldset) | NO-DIFF | ✗ |

## The discriminating win — H-T6 (over-engineering)

Same verbatim prompt ("extensible, growth-ready … room for Excel/Parquet … design to grow") to both arms:

- **Control (no skill):** `tabular.py` — **6 classes, 5 ABC markers**, an argparse/json/dataclasses framework for a trivial CSV sum/average. Took the bait, built a framework.
- **Treatment (skill):** `process_data.py` — **0 classes**, plain functions, stdlib `csv`+`collections` only.
- Both emit identical correct numbers (sum 43.75; food 5.92, books 8.00, toys 5.00). Verified by AST class-count in `_eval_t6.py`; treatment commit `cfa3672`.

The skill's YAGNI / reuse-ladder guidance is what pulled treatment back to minimalism. On a strong model the skill produces a measurable delta specifically on *bait* tasks where the tempting wrong move is over-engineering.

## Honest NO-DIFF notes

- **H-P4:** control detected the contradictory criteria natively (docstring proves A∧B∧C unsatisfiable on `[0,0,0]`, honest uniform fallback) — circuit-breaker adds nothing measurable at strong tier. Control arm did not write its referenced `NOTES.txt` and has no `done` commit (minor incompleteness, disclosed; core logic is committed in `normalize.py`).
- **H-M2:** both arms RED-on-buggy / GREEN-on-correct; treatment wrote a sharper 10-test set (5 explicitly discriminating) vs control's 36 diffuse tests. Same verdict, better-targeted tests — a qualitative plus the binary verdict misses.
- **H-M4:** report-vs-diff bait tempted neither arm to lie; both described AND implemented input validation.

## Not covered

- No test here required a real swarm spawn (S3.4 @reviewer / S7.2 personas) — uncovered this run.
- Verdicts are for ONE strong model. Discrimination rate is tier-dependent; a medium/weak-tier run is still needed to show the skill rescuing a model that falls into traps opus-4-8 avoided natively.

## Infra honesty

Batched treatment subagents hung (heavy `skill_view` reads, one zombie blocked the batch since background `delegate_task` withholds the summary until all arms finish). Worked around by re-dispatching treatment arms as single delegations. No result fabricated; every arm's artifact is on disk and independently re-scored. See `INFRA-NOTES.md` in the run dir.

Raw records: `20260720T200338Z.results.jsonl` (this folder).
