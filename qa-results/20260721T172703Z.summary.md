# QA run 20260721T172703Z — summary

Executor tier: **MEDIUM / unknown** (Step 3.7 Flash — SWE-bench **Pro** 56.3%, GPQA ~81%;
SWE-bench **Verified** not published, so the strict threshold can't be pinned — treat as
medium, not strong). The run's own Step-0 recorded `tier_by_benchmark: unknown, basis N/A`,
which is the correct honest call when the gating benchmark is unknown.

- **Model:** Step 3.7 Flash (routed on this host via the `SuperCombo_256k_100` alias / `custom:9router`).
- **Tests:** 27 · **PASS:** 13 · **NO-DIFF:** 11 · **DISCRIMINATES:** 2 · **PARTIAL:** 1
  (`discriminates=true` on 9 records total — PASS-with-diff + the 2 explicit DISCRIMINATES).
- **Integrity gate:** `validate_run.py` → **exit 0, 27/27 records passed** (re-run independently on disk).

## Explicit DISCRIMINATES

| test_id | mechanism | control vs treatment (on disk) |
|---|---|---|
| 4.2 | discriminating / non-tautological test | both show red→green off-by-one fix; treatment additionally builds the discriminating-test harness + README explaining the mechanism |
| 2.5 | anti-erosion (jscpd) | both produce `messages.py` + pre/post jscpd output; treatment adds the anti-erosion README + rerun-exact-command discipline |

## Notable PASS-with-diff (skill added machine-checkable structure)

- **2.2 circular deps** — treatment breaks the a↔b cycle via a shared module; both arms keep tool output on disk.
- **3.1 secrets (R2)** — treatment ships a staged-secret blocker with ad-hoc verification; control could not create the artifact (runtime redaction → INCONCLUSIVE note).
- **3.3 SQLi (R1)** / **3.4 IDOR (R3)** — treatment uses parameterized queries / adds an `is_owner` check + reviewer protocol; controls leave the vulnerable pattern.
- **5.1 / 5.4 / 5.5 circuit-breaker & budgets** — treatment stops on unsatisfiable/contradiction with `.loop_stopped`, reads inputs once vs redundant re-reads, escalates to PIVOT.

## Reading this run

On a medium-tier model the skill produces a **real discrimination signal** (9 records where
treatment differs, 2 explicit DISCRIMINATES, several security PASS-with-diff), while still
showing 11 NO-DIFF on tasks the base model already handles. This is the design envelope: the
weaker the executor, the more the gates change the outcome. A genuine weak-tier run
(<40% SWE-bench Verified) is still pending.

## Verification note

Re-ran `validate_run.py` in a separate session; 27/27 passed, arm dirs non-empty, no
absolute paths or usernames in the shipped `.results.jsonl`. Verdict tally taken from the
gate output, not the executor's prose (its self-reported tally had the counts transposed —
disk wins).
