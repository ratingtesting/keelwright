# autoresearch-lessons.md — keelwright cross-run learning

> Auto-created by keelwright on first load. Holds lessons promoted from repeated
> failures so the agent doesn't repeat them. The human never needs to touch it.

## Lessons
<!-- Agent appends: pattern → root cause → fix that worked -->
